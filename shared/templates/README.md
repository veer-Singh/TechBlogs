# shared/templates

Reusable post outlines beyond the default `_template/post.mdx` — e.g.
`tutorial.mdx`, `deep-dive.mdx`, `postmortem.mdx`, `benchmark.mdx`.

Copy one over a new blog's `post.mdx` when it fits better than the default.
